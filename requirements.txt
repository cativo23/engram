# Pinned to versions this project was built and tested with (Python 3.14).
fastapi==0.136.3
uvicorn[standard]==0.49.0
openai==2.41.0
pydantic-settings==2.14.1
python-dotenv==1.2.2

# Coming in P2 (ingestion + vector store):
#   psycopg[binary], pgvector, sqlalchemy, alembic, fastembed, numpy
