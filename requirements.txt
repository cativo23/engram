# Pinned to versions this project was built and tested with (Python 3.14).
fastapi==0.136.3
uvicorn[standard]==0.49.0
openai==2.41.0
pydantic-settings==2.14.1
python-dotenv==1.2.2

# P2 — ingestion + vector store  (pinned to Python-3.14-compatible wheels)
psycopg[binary]==3.2.13
pgvector==0.4.2
fastembed==0.8.0
numpy==2.4.6
